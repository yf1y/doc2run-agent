# Doc2Run Agent 使用文档

这份文档说明如何安装项目、复制空模板、准备 API/SDK 文档、运行任务、继续修改代码，以及按需启用场景记忆。

## 1. 安装

需要 Python 3.10 或更高版本。

```bash
git clone https://github.com/yf1y/doc2run-agent.git
cd doc2run-agent
python -m venv .venv
```

激活环境：

```bash
# macOS / Linux
source .venv/bin/activate

# Windows PowerShell
.venv\Scripts\Activate.ps1
```

安装：

```bash
pip install -e .
```

如需运行测试：

```bash
pip install -e ".[dev]"
pytest -q
```

## 2. 复制空模板

仓库中的 `demo/` 不是业务示例，而是一个可以复制的最小项目目录。它不包含模拟 SDK、示例请求或具体场景数据。

```bash
# macOS / Linux
cp -R demo my_project
cp my_project/.env.example my_project/.env

# Windows PowerShell
Copy-Item -Recurse demo my_project
Copy-Item my_project/.env.example my_project/.env
```

复制后目录会直接标明每类知识放在哪里：

```text
my_project/
├── doc2run_agent.yaml
├── .env
└── knowledge/
    ├── api/
    │   ├── setup.md
    │   ├── api_reference.md
    │   └── usage_rules.md
    └── domains/
        └── your_domain/
            ├── docs/
            │   └── domain_knowledge.md
            └── memory_schema.json
```

你可以修改 `my_project`。如果使用垂直领域，把 `your_domain` 改成启动参数 `--domain` 的值；不需要领域资料和场景记忆时，可以删除整个 `domains/`。

## 3. 配置模型 API

编辑 `my_project/doc2run_agent.yaml`：

```yaml
models:
  defaults:
    model: your-provider/your-model
    api_key_env: MODEL_API_KEY
    timeout: 120
    max_retries: 3
    max_tokens: 4000
    context_tokens: 16000
  requirements:
    max_tokens: 1500
  code:
    timeout: 180
    max_tokens: 5000
  fix:
    max_tokens: 3000
```

`model` 使用 LiteLLM 支持的模型名。`requirements`、`code` 和 `fix` 可以各自覆盖模型、服务地址、超时和 token 配置；没有覆盖的值继承 `defaults`。

编辑同目录的 `.env`：

```dotenv
MODEL_API_KEY=填写真实密钥
```

程序会从配置文件所在目录读取 `.env`。不要把密钥直接写进 YAML、知识文档、自然语言请求或生成代码，也不要提交 `.env`。

`max_retries` 是模型调用的最大重试次数。`max_tokens` 是输出上限；程序会用 `context_tokens - max_tokens` 计算输入可用空间，超出时明确停止，不会偷偷删掉需求、接口签名或当前代码。

## 4. 准备三类知识

程序不会把所有材料混在一个目录里。三类内容分别检索、分别交给模型，并分别保存本次实际使用的内容。

| 内容 | 放置位置 | 解决的问题 | 谁来维护 |
|---|---|---|---|
| API/SDK 资料 | `knowledge/api/` | 代码具体怎样调用 | 用户事先填写 |
| 领域资料 | `knowledge/domains/<domain>/docs/` | 业务上什么结果才对 | 用户或领域负责人事先填写 |
| 已验收场景 | `memory/approved/<domain>/` | 过去有哪些同领域结果已确认正确 | 程序在双重确认后写入 |

前两类支持 Markdown、TXT、JSON 和 JSONL。模板 Markdown 只有 HTML 注释，检索时会忽略；如果用户没有填入真实 API 资料，程序会在启动时明确报错。

### 4.1 API/SDK 资料

`knowledge/api/` 是必需目录。模板将内容拆成：

- `setup.md`：安装包、版本、import、客户端初始化、鉴权变量名和关闭方式；
- `api_reference.md`：完整签名、参数、返回结构、异常、副作用和最小调用片段；
- `usage_rules.md`：调用顺序、分页、限流、超时、重试、并发和资源释放规则。

文件可以继续拆分或改名，程序按内容检索；但不要把业务布局、标准拓扑等领域数据塞到接口文件里。

### 4.2 领域资料

领域资料是可选的，但只要任务依赖业务事实，就应该提供。例如电力场景中的节点含义、标准连接关系、布局、计算约束和参数表，应放在：

```text
knowledge/domains/power/docs/
```

并使用 `--domain power` 启动。目录名与参数必须完全一致。领域资料应写明：

- 对象、字段和术语的准确含义；
- 业务规则、计算规则和边界条件；
- 标准布局、映射、拓扑、参数表等参考数据；
- 哪些值可以由模型设计，哪些必须严格照文档使用；
- 资料版本、适用范围和来源。

如果接口资料只说明 `create_node()` 怎样调用，却没有 33 节点的连接关系，模型无法准确还原布局。连接关系应由领域资料提供；如果是某次运行中新确认的具体场景，也可以在验收后进入场景记忆。

### 4.3 已验收场景

`memory/approved/<domain>/` 不由用户手工填写。它只接收当前领域中已经运行成功、被用户 `/approve`、通过固定格式检查、通过独立审查，并最终 `/remember` 的内容。

接口签名、import、源代码、凭证、报错和修复过程都不能进入场景记忆。

不要写入：

- API Key、Token、密码和生产连接串；
- 真实用户数据或其他敏感内容；
- 已失效的旧版本签名；
- 来源不明、无法核对的业务数据。

## 5. 运行任务

在仓库根目录运行：

```bash
doc2run-agent \
  --session my-project \
  --config my_project/doc2run_agent.yaml \
  --knowledge-dir my_project/knowledge
```

Windows PowerShell 可以写成一行：

```powershell
doc2run-agent --session my-project --config my_project/doc2run_agent.yaml --knowledge-dir my_project/knowledge
```

启动后直接输入需求。模板不提供 `request.txt`，因为每个用户的请求本来就不同。

建议一次说清：

- 想完成什么；
- 输入来自哪里；
- 输出到哪里、格式是什么；
- 是否允许写文件或修改远程数据；
- 可以使用哪些依赖；
- 怎样判断运行成功。

Agent 可能继续追问。需求整理完成后先查看展示的内容，确认无误再输入：

```text
/confirm
```

随后程序会检索资料、写实现方案、核对方案、按需补查文档、生成代码、检查并实际运行。如果最终方案核对仍未通过，程序不会放行代码生成；补充文档后可再次输入 `/confirm`。

启动信息会明确显示本次加载的路径，例如：

```text
API documentation: my_project/knowledge/api
Domain documentation (power): my_project/knowledge/domains/power/docs [loaded]
Approved scenario memory: memory/approved/power
```

## 6. 成功后继续修改

代码第一次运行成功不代表会话立即结束。你可以继续输入普通文字，例如要求调整输出格式、增加过滤条件或修改文件名。系统会在当前代码和运行记录上：

```text
确定改动位置 → 修改相关代码 → 核对修改 → 重新测试
```

满意后输入：

```text
/approve
```

因此，继续修改仍属于原来的代码处理过程；`/approve` 才表示用户接受当前版本。

## 7. session 保存与恢复

默认数据保存在 `sessions/<session-id>/`：

```text
session.json       对话和当前阶段
decisions.md       用户确认、补充和纠正
task_specs/        已确认的需求版本
retrieval/         文档检索结果
planning/          接口资料、领域资料、历史场景及方案核对结果
contexts/          每次实际发送给模型的输入输出
runs/              每轮代码、检查、stdout、stderr 和修改记录
workspace/         当前生成脚本及相对路径输出
```

退出后使用相同的 `--session`、`--config` 和 `--knowledge-dir` 即可继续。`/reset` 会把旧 session 移入归档目录，不会直接删除。

## 8. 可选的场景记忆

不传 `--domain` 时，领域资料检索以及记忆的检索和写入都关闭。需要启用时，填写模板中的：

```text
my_project/knowledge/domains/<domain>/docs/
my_project/knowledge/domains/<domain>/memory_schema.json
```

文件用于固定这个领域允许保存什么。例如其结构为：

```json
{
  "schema_version": "1",
  "scenario_kind": "填写稳定的场景类型",
  "required_fields": ["填写必需字段"],
  "allowed_fields": ["填写允许字段"],
  "field_types": {
    "填写字段名": "array"
  },
  "forbidden_keys": ["填写本领域额外禁止的字段"]
}
```

字段类型支持项目校验器接受的 JSON 类型。这个文件必须由领域负责人先定义，而不是让模型临时创造规则。核心校验还会拒绝 API、函数签名、import、源码、凭证和修复过程等内容。

启动时增加领域和记忆目录：

```bash
doc2run-agent \
  --session my-project \
  --config my_project/doc2run_agent.yaml \
  --knowledge-dir my_project/knowledge \
  --domain your_domain \
  --memory-dir memory
```

记忆流程是：

1. 代码运行成功，用户输入 `/approve`；
2. 程序在新的模型上下文中提取当前领域的候选，不读取 Fix 对话；
3. 固定规则检查候选格式和禁止内容；
4. 独立审查检查它是否被成功代码和用户确认支持；
5. 用户查看后输入 `/remember` 才正式保存，或输入 `/reject-memory` 拒绝。

程序启动时就会读取该领域的 `memory_schema.json`，不存在或格式错误会立即停止。只有完全相同的 `--domain` 会检索该领域资料和历史场景。一个领域的知识不会进入其他领域，接口知识也不会因为一次代码验收而增加。

## 9. 常用命令

```text
/show             查看当前需求
/history          查看需求对话
/confirm          确认需求，或在中断后重新尝试
/approve [说明]   验收当前成功版本，可选生成场景候选
/remember         保存通过检查和审查的候选
/reject-memory    拒绝并归档候选
/reset            归档当前 session 并重新开始
/help             查看帮助
/exit             保存并退出
```

## 10. 安全边界

当前本地 Runner 会检查语法、部分危险调用、明显的越界写入，并限制环境变量和运行时间，但它不是容器或虚拟机级沙箱。

适合在可信本机环境试用的任务包括查询、筛选、报表导出、配置检查、私有 SDK 调用和明确验收条件下的低频脚本。不应直接用于不可信输入的公共执行服务、无人确认的高风险写操作或需要强隔离的生产任务。

生产化时应替换为受限容器或虚拟机，并明确限制文件系统、网络、子进程、资源使用和凭证注入。

## 11. 常见问题

### 提示知识目录为空

确认 `--knowledge-dir` 指向项目中的 `knowledge/`，并且 `knowledge/api/` 下已经用真实内容替换了占位注释。API 文件不能放在 `knowledge/` 根目录或领域目录。

### 模型一直追问

补充输入、输出格式、限制条件、副作用和可以实际验证的成功标准。

### 方案核对失败

查看 `sessions/<session-id>/planning/plan_review.json` 和 `generation_notes.md`。如果缺少接口签名、返回字段或调用顺序，先补文档，再重新 `/confirm`，不要让模型靠重复尝试猜接口。

### 生成代码运行失败

确认真实 SDK 安装在当前虚拟环境，并查看该 session 的 `runs/`、`stdout.txt` 和 `stderr.txt`。系统会在限定次数内尝试修改；超过限制后保留全部材料供继续排查。
