# Doc2Run Agent 使用文档

Doc2Run Agent 的知识边界只有两类：API/SDK 知识和 Scene 知识。

## 1. 安装

需要 Python 3.10+：

```bash
git clone https://github.com/yf1y/doc2run-agent.git
cd doc2run-agent
python -m venv .venv
source .venv/bin/activate       # Windows PowerShell: .venv\\Scripts\\Activate.ps1
pip install -e .
```

## 2. 准备知识目录

```text
my_project/
├── doc2run_agent.yaml
├── .env
└── domain_knowledge/
    ├── api/                         # Code/Fix 阶段按关键词检索
    │   ├── setup.md
    │   ├── api_reference.md
    │   └── usage_rules.md
    └── scenes/                      # Chat 阶段按文档检索，只选一个并全文注入
        ├── scene_5_nodes.md
        └── scene_33_nodes.md
```

两类知识均支持 `.md`、`.txt`、`.json`、`.jsonl`、`.yaml`、`.yml`。API 文档会切分为可检索片段；Scene 按文件建立文档级索引，选中后使用该文件全文。

Scene 文档应描述场景本身，而不是接口调用：

```markdown
# 场景目标

# 器件清单

# 排布与连接关系

# 节点编号与参数

# 固定不变量

# 泛化规则

# 输出与验收标准
```

例如用户要求 33 节点时，Chat 仍可以检索并注入 5 节点 Scene，然后在方案中明确 5→33 的扩展规则。没有完全匹配的 Scene 时，检索器会选相似度最高的一个；不会把多个 Scene 拼接进上下文。

## 3. 配置模型

`doc2run_agent.yaml`：

```yaml
models:
  defaults:
    model: your-provider/your-model
    api_key_env: MODEL_API_KEY
    timeout: 120
    max_retries: 3
    max_tokens: 4000
    context_tokens: 16000
  chat:
    max_tokens: 2000
  code:
    timeout: 180
    max_tokens: 5000
  fix:
    max_tokens: 3000
```

`max_retries` 由 LiteLLM 传给 provider；`max_tokens` 限制每个阶段的输出长度。模型密钥放在同目录 `.env`，不要写入文档或代码。

## 4. 启动和交互

```bash
doc2run-agent \\
  --session my-project \\
  --config my_project/doc2run_agent.yaml \\
  --knowledge-dir my_project/domain_knowledge
```

如果不传 `--session`，启动时会列出已有会话名：输入编号继续已有会话，输入 `n` 创建新会话，输入 `q` 退出；没有历史会话时会直接询问新会话名。传入已有的 `--session` 会直接继续它；传入不存在的名称时，程序会先询问是否创建。

交互路径：

```text
用户需求
  ↓
Chat：Scene 文档级 Top 1 检索、全文注入、澄清需求并生成开放结构 Scenario Plan
  ↓
Code：/confirm 冻结 TaskSpec 和完整方案，再按器件、动作、参数检索 API 并生成执行
  ↓
Fix：静态校验或执行失败时按 API 文档局部修复、复核并复测
  ↓
Memory：/approve 将确认方案直接保存为 scenes/<name>-<hash>.md
```

常用命令：

```text
/show       查看 TaskSpec 和方案草稿
/history    查看 Chat 对话
/confirm    确认需求和方案并开始生成
/approve    运行 Memory：验收成功代码并把方案沉淀为 Scene
/reset      归档当前 session，开始新任务
/exit       保存并退出
```

`/confirm` 前必须有完整目标、输入输出、约束、验收标准和结构化方案。方案不强制固定字段，但必须有 Markdown 标题和正文，至少说明场景结构及执行结果如何验收。

## 5. 运行产物

```text
sessions/<session-id>/
├── session.json
├── decisions.md
├── task_specs/task_spec_vN.json
├── planning/
│   ├── selected_scene.md
│   ├── scenario_plan.md
│   ├── confirmed_plan.md
│   └── api_context.md
├── retrieval/                     # API 检索结果
├── contexts/                      # 实际模型输入输出
├── runs/                          # 代码、校验、stdout、stderr
└── workspace/                     # 当前脚本运行目录
```

`scenes/` 中的文件既可以由用户预先维护，也可以由 `/approve` 直接产生。沉淀内容是确认后的场景方案，不包含 API 签名、import、源码、密钥、错误堆栈或修复过程。

## 6. 安全边界

Runner 只提供规则校验、超时和显式运行环境变量白名单，不是操作系统级沙箱。请只在可信本机运行；生产环境应使用受限容器或虚拟机，并独立限制文件系统、网络、子进程和凭证权限。
