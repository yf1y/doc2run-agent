# Doc2Run Agent 使用文档

这份文档从安装开始，说明如何准备可检索文档、运行仓库 Demo、替换为自己的项目，
以及如何继续修改和保存场景知识。

## 1. 安装

需要 Python 3.10 或更高版本。

```bash
git clone https://github.com/yf1y/doc2run-agent.git
cd doc2run-agent
python -m venv .venv
```

激活环境：

```bash
# macOS / Linux
source .venv/bin/activate

# Windows PowerShell
.venv\Scripts\Activate.ps1
```

安装项目：

```bash
pip install -e .
```

如需运行测试：

```bash
pip install -e ".[dev]"
pytest -q
```

## 2. 配置模型

复制示例配置和环境变量文件：

```bash
# macOS / Linux
cp config.example.yaml doc2run_agent.yaml
cp .env.example .env

# Windows PowerShell
Copy-Item config.example.yaml doc2run_agent.yaml
Copy-Item .env.example .env
```

最小配置可以让所有阶段使用同一个 LiteLLM 模型名：

```yaml
models:
  defaults:
    model: openai/gpt-5
    api_key_env: OPENAI_API_KEY
    timeout: 120
    max_retries: 3
    max_tokens: 4000
    context_tokens: 16000
```

在 `.env` 中配置对应变量，不要把真实密钥写入 YAML、文档、请求或 Git：

```dotenv
OPENAI_API_KEY=your-key-here
```

`max_retries` 直接交给 LiteLLM，不再叠加一层项目级模型重试；`max_tokens` 是输出上限，
`context_tokens - max_tokens` 才是一次调用可使用的输入预算。requirements、code、fix
可以在 YAML 中分别覆盖这些值。

## 3. 准备文档

推荐每个项目使用一个独立目录：

```text
my_project/
├── request.txt
└── knowledge/
    ├── api/
    │   ├── sdk_overview.md
    │   ├── api_reference.json
    │   └── examples.md
    └── domains/                         # 不使用场景记忆时可以没有
        └── your_domain/
            └── memory_schema.json
```

本地检索支持 `.md`、`.txt`、`.json` 和 `.jsonl`。当 `knowledge/api/` 存在时，接口检索
只读取这个目录；`knowledge/domains/` 中的 schema 不会被当作接口说明检索。

### 3.1 API/SDK 文档至少应包含什么

模型能否写对代码，很大程度取决于文档是否明确。建议包含：

- 正确的安装包名和 `import` 写法；
- 类、函数和方法的完整名称与签名；
- 参数类型、是否必填、默认值和取值范围；
- 返回值结构，特别是字段名和类型；
- 调用顺序、分页方式和空结果行为；
- 可能抛出的异常；
- 哪些调用会读数据、写文件或修改远程状态；
- 一个最小可运行示例，以及必要的边界示例。

不要只放营销介绍或只有接口名称的目录。对于“33 节点布局”这类具体场景，如果 API 文档
只说明怎样创建节点，就不能期待模型从接口签名还原准确布局；场景数据应由用户提供，或在
结果验收后通过场景记忆逐步积累。

### 3.2 文档中不要放什么

- API Key、Token、密码和真实账号；
- 生产环境连接串；
- 无法确认来源的“标准数据”；
- 与当前 SDK 版本不一致的旧签名；
- 希望模型偷偷执行、但 TaskSpec 中没有确认的副作用。

当前 `LocalPythonRunner` 只保留精简环境变量，不提供真实 SDK 凭证注入机制。需要凭证的
生产 SDK 应先接入受控凭证注入和容器/VM Runner，不能把凭证写进生成代码。

### 3.3 可选场景记忆

场景记忆不是接口文档。每个领域必须提供固定 schema，例如：

```json
{
  "schema_version": "1",
  "scenario_kind": "power_network",
  "required_fields": ["nodes", "connections"],
  "allowed_fields": ["nodes", "connections", "loads", "parameters"],
  "field_types": {
    "nodes": "array",
    "connections": "array",
    "loads": "array",
    "parameters": "object"
  },
  "forbidden_keys": ["endpoint", "request", "response"]
}
```

核心校验还会禁止 `api`、`signature`、`import`、`code`、`function`、`method`、
`credentials` 等字段。场景只在完全相同的 `--domain` 下检索。

## 4. 运行仓库 Demo

仓库的 [`demo/`](demo/) 是一套完整、无需账号的示例。启动：

```bash
doc2run-agent --session demo --knowledge-dir demo/knowledge
```

将 [`demo/request.txt`](demo/request.txt) 的内容粘贴到 CLI。需求整理完成后，检查展示的
TaskSpec，然后输入：

```text
/confirm
```

程序会依次完成接口检索、实现方案、方案核对、代码生成、静态检查和实际执行。成功后可以：

- 直接输入普通文字继续修改代码，系统会局部改写、核对并重新运行；
- 输入 `/approve` 验收当前版本并结束代码修改；
- 输入 `/reset` 归档当前 session 后重新开始。

如需体验场景记忆，启动时增加：

```bash
doc2run-agent \
  --session demo-memory \
  --knowledge-dir demo/knowledge \
  --domain demo_records \
  --memory-dir memory
```

成功后输入 `/approve` 只会生成候选。查看候选和审查结果后，再输入 `/remember` 才会放入
`memory/approved/demo_records/`；输入 `/reject-memory` 会将候选归档到 rejected 目录。

## 5. 替换成自己的项目

复制 Demo：

```bash
# macOS / Linux
cp -R demo my_project

# Windows PowerShell
Copy-Item -Recurse demo my_project
```

然后：

1. 清空并替换 `my_project/knowledge/api/` 中的文档；
2. 修改 `my_project/request.txt`；
3. 确保真实 SDK 已安装在当前 Python 环境；
4. 使用新目录启动。

```bash
doc2run-agent --session my-project --knowledge-dir my_project/knowledge
```

如果需要领域记忆，再复制并修改 `knowledge/domains/<domain>/memory_schema.json`，启动时传入
同名 `--domain`。一个 session 在首次确认 TaskSpec 后会锁定领域，恢复时必须使用相同参数。

## 6. 常用命令

```text
/show             查看当前 TaskSpec
/history          查看需求对话
/confirm          确认 TaskSpec，开始生成和运行；中断后也用于重试
/approve [说明]   验收当前成功版本，可选生成场景候选
/remember         将通过检查和审查的候选加入当前领域
/reject-memory    拒绝并归档候选
/reset            归档 session 并重新开始
/help             查看帮助
/exit             保存并退出
```

如果最终实现方案复核仍未通过，系统不会生成代码。它会保存检索、方案和审查结果，保持
已确认 TaskSpec，并提示使用 `/confirm` 重新尝试。

## 7. 查看结果与恢复

默认运行产物保存在 `sessions/<session-id>/`：

```text
session.json       对话、阶段和恢复状态
task_specs/        已确认的 TaskSpec 版本
retrieval/         每轮检索结果
planning/          API/场景上下文、实现方案和方案审查
contexts/          实际模型输入与输出
runs/              每次代码、校验、执行和修改记录
workspace/         最终执行脚本及相对路径输出
```

重新运行相同的 `--session` 会恢复状态。`/reset` 不会直接删除旧记录，而是移动到
`sessions/archives/`。

## 8. 适用范围与安全边界

适合：数据查询、筛选、报表导出、配置检查、私有 SDK 示例和有明确验收条件的低频脚本。

不适合直接用于：不可信输入的公共执行服务、生产级调度、高风险无人确认写操作，以及需要
强安全隔离或真实凭证的任务。

当前 AST 检查是执行前策略检查，不是安全沙箱。生成代码应只在可信本地环境试用；生产部署
需要将 `LocalPythonRunner` 替换为限制文件系统、网络、子进程和资源用量的容器或虚拟机。

## 9. 常见问题

### 文档目录不存在或为空

确认 `--knowledge-dir` 指向包含 `.md`、`.txt`、`.json` 或 `.jsonl` 文件的目录。推荐把
接口文档放在它的 `api/` 子目录中。

### 模型一直追问

请求中应明确目标、输入、输出位置、允许依赖、副作用和可验证的成功标准。可以参考
`demo/request.txt` 的写法。

### 方案复核失败

先查看 `sessions/<session-id>/planning/plan_review.json` 和 `generation_notes.md`。如果缺少
接口签名或返回字段，补充文档后再次 `/confirm`；不要靠多次重试让模型猜接口。

### 代码可以生成但运行失败

确认 SDK 已安装在创建虚拟环境的同一 Python 解释器中，并查看该 session 的 `runs/`、
`stdout.txt` 和 `stderr.txt`。
