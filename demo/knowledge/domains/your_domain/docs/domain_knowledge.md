<!--
将目录名 your_domain 改为实际 --domain 名称，删除本注释并填写该领域的已知事实：

1. 领域中的对象、字段和术语分别表示什么；
2. 必须遵守的业务规则、计算规则和边界条件；
3. 已有的标准布局、映射、拓扑、参数表或其他参考数据；
4. 哪些内容可以由模型设计，哪些必须严格使用文档给定值；
5. 资料的版本、适用范围和来源。

这里不写 API 签名，不写模型生成的代码，也不写未经确认的猜测。
可在本 docs/ 目录中增加 Markdown、TXT、JSON 或 JSONL 文件。
-->
