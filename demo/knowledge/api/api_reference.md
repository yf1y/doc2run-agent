<!--
删除本注释，填写 API/SDK 的准确接口说明：

1. 类、函数和方法的完整名称与签名；
2. 参数类型、是否必填、默认值和取值范围；
3. 返回值类型、字段结构、空结果表现和异常；
4. 每个接口是否会写文件或修改远程数据；
5. 可直接运行的最小调用片段。

不要在文档中写入真实密钥、令牌或生产数据。
可以拆成多个 Markdown、TXT、JSON 或 JSONL 文件。
-->
