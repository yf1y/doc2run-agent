<!--
删除本注释并填写项目真实资料。建议至少包含：

1. 安装方式和 Python 导入路径；
2. 类、函数和方法的完整签名；
3. 参数类型、返回值结构和异常；
4. 初始化、鉴权及必要环境变量；
5. 可直接运行的最小调用片段；
6. 分页、限流、重试、资源释放等约束。

不要在文档中写入真实密钥、令牌或生产数据。
可以在 knowledge/api/ 下增加更多 Markdown、JSON 或 YAML 文件。
-->
