{
  "schema_version": "1",
  "scenario_kind": "power_network",
  "required_fields": ["nodes", "connections"],
  "allowed_fields": ["nodes", "connections", "loads", "parameters"],
  "field_types": {
    "nodes": "array",
    "connections": "array",
    "loads": "array",
    "parameters": "object"
  },
  "forbidden_keys": ["endpoint", "request", "response"]
}
